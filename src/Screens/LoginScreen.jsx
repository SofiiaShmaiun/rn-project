import React, { useState } from "react";
import {
  StyleSheet,
  Text,
  ScrollView,
  TextInput,
  Button,
  View,
  Image,
  Pressable,
} from "react-native";

import Svg, { Path } from "react-native-svg";

export default function RegistrationScreen({ onLogin }) {
  // const [text, setText] = useState("");

  return <Text>Реєстрація</Text>;
}
